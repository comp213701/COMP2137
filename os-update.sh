#!/bin/bash
# Update the operating system software.
# Do not ask the person running the script any questions,
# although it may need the user to enter a password for sudo use.

sudo apt update
sudo apt upgrade
